# 独立调度器 — 功能扩展计划表

> 当前版本：v0.3（已实现：提交/队列/Worker/重试/去重/死信/持久化/历史查询/状态监控/LLM调用）

---

## 阶段四：体验增强（按优先级排序）

### P0 - 核心效率

| # | 功能 | 说明 | 预估工时 |
|---|------|------|---------|
| 1 | **Web 监控面板** | 简单的 HTML 看板：任务量曲线、成功率、Worker 负载、队列深度。FastAPI 挂载静态页面，JS 轮询 `/status` 和 `/tasks/history` | 1天 |
| 2 | **定时任务 / Cron** | arq 原生支持 `cron_jobs`。在 WorkerSettings 中加入定时清理、定期巡检等内置任务 | 0.5天 |
| 3 | **取消任务 API** | `DELETE /tasks/{job_id}` — 删除还在队列中或运行中的任务（Redis 标记取消，Worker 轮询时跳过） | 0.5天 |
| 4 | **手动重试 API** | `POST /tasks/{job_id}/retry` — 对已失败的任务重新入队 | 0.5天 |

### P1 - 能力扩展

| # | 功能 | 说明 | 预估工时 |
|---|------|------|---------|
| 5 | **真正的多队列优先级** | 当前优先级只是标记，arq 只支持单一队列。改为 Redis 多队列 + Worker 按优先级消费（P0 队列空才消费 P1） | 1天 |
| 6 | **令牌桶限速** | 控制调用 DeepSeek API 的频率，防止触发 429。asyncio.Semaphore + 令牌桶算法 | 1天 |
| 7 | **WebSocket 推送** | 任务状态变更时主动推送给前端，替代轮询。FastAPI 的 WebSocket 支持 | 1天 |
| 8 | **任务依赖 / DAG** | B 依赖 A → A 完成后自动触发 B。Redis 记录依赖关系，Worker 完成时检查下游 | 1.5天 |

### P2 - 运维与集成

| # | 功能 | 说明 | 预估工时 |
|---|------|------|---------|
| 9 | **API Key 认证** | 给调度器加简单的鉴权，防止未授权调用 | 0.5天 |
| 10 | **结果导出** | `GET /tasks/export?format=json` — 导出任务结果 | 0.5天 |
| 11 | **通知集成** | 任务完成时通过 callback_url 或新通道（微信/邮件）发送通知 | 1天 |
| 12 | **Docker 化** | Dockerfile + docker-compose（scheduler + Redis + Worker） | 0.5天 |

### P3 - 长期愿景

| # | 功能 | 说明 | 预估工时 |
|---|------|------|---------|
| 13 | **多机 Worker 集群** | 多台机器从同一 Redis 拉任务，水平扩展 | 2天 |
| 14 | **性能仪表盘** | 任务耗时分布、P50/P95/P99 延迟、成功率趋势图 | 2天 |
| 15 | **日志聚合** | 集中收集 Worker 日志，支持按时间/级别/任务类型筛选 | 1天 |

---

## 推荐路线

```
第一优先（阶段四 P0）：监控面板 + Cron + 取消/重试 API
   ↓ 核心体验闭环
第二优先（P1）：多队列 + 限速 + WebSocket
   ↓ 容量与实时性
第三优先（P2）：认证 + 导出 + 通知 + Docker
   ↓ 可交付
第四优先（P3）：集群 + 仪表盘 + 日志
   ↓ 企业级
```

## 当前推荐下一步

**Web 监控面板**是最值得做的——现在所有数据都有 API，只缺一个前端把它们串起来展示。有了面板，调度器的运行状态一目了然。要焊吗？
